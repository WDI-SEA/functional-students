import React from "react"

const Score = (props) => { 

    return (
      <div>
        <h4>Score: {props.score}</h4>
        <h6>Date: {props.date}</h6>
   
      </div>
    )
  }


  export default Score